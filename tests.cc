﻿#include <iostream>
#include <vector>
#include <include.h>

using namespace std;
using namespace sorting;

int main() {
	vector<int> mas{ 5,1,2,3,4 };
	cout << mas << endl;
	//cout << insert_sort(mas);
	//cout << coctail_sort(mas);
	//cout << heap_sort(mas);
	//cout << mas << endl;
	return 0;
}